"""ClearQueue — accounts-payable exception triage.

An agent that clears the invoices the ERP's automatic match could not, and hands each one
to a named human as a signable review packet. It recommends; a person decides.
"""

__version__ = "1.0.0"
